# Sway

This is my config for the [Sway](https://swaywm.org/) tiling window manager.

## Installation

Clone the repo into your `.config` folder:

```bash
cd ~/.config && git clone https://github.com/rednokay/sway
```
